# Supabase Configuration (do not commit real values)
VITE_SUPABASE_URL=your_supabase_url
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key

# JWT Secret (used by Netlify Functions, set as JWT_SECRET in Netlify UI)
VITE_JWT_SECRET=for-local-only-if-needed

# Note: Set production secrets in Netlify environment variables, not in the repo.
